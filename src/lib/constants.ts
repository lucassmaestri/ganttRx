// Static fallback — Timeline and Histogram use viewStore.viewStart/viewEnd instead.
// These are only kept for any external imports that may reference them.
export const VIEW_START = new Date(2000, 0, 1);
export const VIEW_END   = new Date(2100, 11, 31);
